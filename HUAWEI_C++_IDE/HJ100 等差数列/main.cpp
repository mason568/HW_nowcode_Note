#include<iostream>
using namespace std;

int main() {
	int n;
	while (cin >> n) {
		//int a1 = 2, d = 3;
		//int an = a1 + (n - 1) * d;
		//int sn = (a1 + an) * n / 2;
		cout << (1+3*n)*n/2 << endl;
	}
}